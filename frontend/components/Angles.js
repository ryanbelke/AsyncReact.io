import Angle from "../components/styles/Angle";

const Angles = props => (
  <Angle top={props.top} id="angles">
    <span />
    <span />
    <span />
    <span />
    <span />
  </Angle>
);

export default Angles;
